<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"

    android:paddingTop="@dimen/activity_vertical_margin" android:background="@drawable/background"
    tools:context=".MainActivity">


    <ListView android:id="@+id/chat_list_view"  android:paddingRight="@dimen/activity_horizontal_margin" android:paddingLeft="@dimen/activity_horizontal_margin" android:divider="@drawable/chat_divider"
        android:layout_width="match_parent" android:scrollbarStyle="outsideOverlay"
        android:layout_height="match_parent"></ListView>

    <LinearLayout android:background="@android:color/white"
        android:layout_width="match_parent" android:layout_alignParentBottom="true"
        android:layout_height="wrap_content">
    <EditText
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:id="@+id/chat_edit_text1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scrollHorizontally="false"
        android:layout_weight="1"

        android:hint="@string/type_your_message"
        android:maxLines="4"
        android:singleLine="false"
        android:inputType="textCapSentences"

        android:textSize="18sp"
        android:paddingLeft="4dp"/>

            <ImageView
                android:id="@+id/enter_chat1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
               android:layout_gravity="center_vertical"
                android:paddingLeft="13dp"
                android:paddingRight="17dp"
                android:src="@drawable/ic_chat_send" />

    <EditText
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:id="@+id/chat_edit_text2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scrollHorizontally="false"
        android:layout_weight="1"
        android:hint="@string/type_your_message"
        android:maxLines="4"
        android:singleLine="false"
        android:inputType="textCapSentences"

        android:textSize="18sp"
        android:paddingLeft="4dp"/>

        <ImageView
            android:id="@+id/enter_chat2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:paddingLeft="13dp"
            android:paddingRight="17dp"
            android:src="@drawable/ic_chat_send" />

    </LinearLayout>

</RelativeLayout>
